procs:
  infrastructure:
    shell: ". ./.env; cd mineral-flow-system/infrastructure; docker-compose up"
    stop: { send-keys: ["<C-c>"] }
    autostart: false
  mineral-flow-system:
    shell: ". ./.env; cd mineral-flow-system; gradle bootRun"
    stop: { send-keys: ["<C-c>"] }
    autostart: false
  techtopia-frontend:
    shell: ". ./.env; cd techtopia-frontend/techtopia-frontend; npm run dev"
    stop: { send-keys: ["<C-c>"] }
    autostart: false
  backend-entrance-gate:
    shell: ". ./.env; cd Warehousing; gradle :Warehousing:bootRun"
    stop: { send-keys: ["<C-c>"] }
    autostart: false
  backend-park-info-sys:
    shell: ". ./.env; cd Landside; gradle :Landside:bootRun"
    stop: { send-keys: ["<C-c>"] }
    autostart: false
  backend-park-ops:
    shell: ". ./.env; cd Waterside; gradle :Waterside:bootRun"
    stop: { send-keys: ["<C-c>"] }
    autostart: false
  backend-ticketing:
    shell: ". ./.env; cd Invoicing; gradle :Invoicing:bootRun"
    stop: { send-keys: ["<C-c>"] }
    autostart: false
